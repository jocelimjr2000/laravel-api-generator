<?php

use App\Http\Controllers\{{ controller }};
use Illuminate\Support\Facades\Route;

Route::prefix('{{ prefix }}')
    ->controller({{ controller }}::class)
    ->group(function () {

{{ routes }}
    });
